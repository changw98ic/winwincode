# 安全报告

## 支持范围

最新公开发布版本接收安全修复。`main` 分支的当前源码预览也接收报告；尚未进入稳定版的数据结构会在发布前整理为当时的当前结构。

以下内容属于本仓库的安全范围：

- WinWinCode 的 Delivery、阶段、Attention、Evidence 和 Verdict 处理；
- DSH Session 与 Codex Session 的绑定、模型回调和事件投影；
- 内嵌 Codex Core 的补丁、权限传递、沙箱边界和原生模块；
- 安装包、构建脚本、发布证据及凭据隔离。

上游 Codex 或 DSH 本身的问题也可以先在本仓库私下报告。维护者会确认问题位于 WinWinCode 集成、固定版本还是上游源码，再与对应上游协调。

## 私下提交

使用 GitHub 的 [WinWinCode 私有漏洞报告表单](https://github.com/changw98ic/winwincode/security/advisories/new)。该仓库已经启用 Private Vulnerability Reporting；报告内容只在报告者和仓库安全维护者之间可见。

请提供：

1. 受影响的 WinWinCode 版本或完整源码 commit；
2. 操作系统、CPU 架构和 Node/Rust 版本；
3. 问题影响、成立条件和最小复现步骤；
4. 可以安全分享的日志片段、调用顺序或示例数据；
5. 已知缓解方式或修复建议（如有）。

报告中不要放入真实 API Key、访问令牌、个人数据或第三方机密。可以用占位值说明字段位置。

在维护者完成确认并与报告者约定披露时间前，不要通过公开 Issue、Pull Request、Discussion 或社交媒体发布漏洞细节。报告者不需要先公开问题，也不需要先准备修复。

## 处理过程

维护者会在同一私有线程中完成以下状态更新：

1. 确认收到并检查复现信息；
2. 判断影响范围、受影响版本和上游归属；
3. 准备修复、验证四个平台中受影响的目标，并重新生成需要的发布证据；
4. 与报告者约定公告内容和时间；
5. 发布修复版本和 GitHub Security Advisory。

具体完成时间取决于问题影响、复现条件和上游协调情况。维护者不会要求报告者在修复前公开技术细节。

## 安全发布

安全修复使用新的版本号和正常的 [发布流程](docs/releasing.md)。已经发布的包不会被同版本文件覆盖；受影响版本会在需要时标记为不建议使用，修复通过下一版本交付。
