# winwincode-integration-core

Product-neutral integration identities, scoped authority, receipts, retry state,
and storage and provider ports. The crate owns no database driver, credential
value, callback URL, or product deployment policy.
