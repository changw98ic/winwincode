# winwincode-test-assets

Deterministic, read-only analysis of baseline and candidate test assets. The
crate reports structured findings with source paths and content digests; policy
enforcement and final verification remain outside this crate.
